import { NoEmptySpacesAllowedValidator } from './no-empty-spaces-allowed-validator';

describe('NoEmptySpacesAllowedValidator', () => {
  it('should create an instance', () => {
    expect(new NoEmptySpacesAllowedValidator()).toBeTruthy();
  });
});
