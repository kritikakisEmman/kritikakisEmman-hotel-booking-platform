import { ReservationTypeRequest } from './reservation-type-request';

describe('ReservationTypeRequest', () => {
  it('should create an instance', () => {
    expect(new ReservationTypeRequest()).toBeTruthy();
  });
});
